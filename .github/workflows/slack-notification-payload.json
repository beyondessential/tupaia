{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "${{ job.status == 'success' ? ✅ : ❌ }} CI: ${{ job.status }}\n${{ github }}"
      }
    }
  ]
}
