name: Review Hero
on:
  pull_request:
    # synchronize is needed so the workflow appears in PR status checks on every push
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    uses: beyondessential/review-hero/.github/workflows/review.yml@main
    secrets: inherit
