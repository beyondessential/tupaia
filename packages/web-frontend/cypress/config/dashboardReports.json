{
  "_description": [
    "A list of urls that will be used for testing dashboard reports.",
    "It can either be manually populated, or generated using the contents of configuration files.",
    "Those can be specified using `tupaia.dashboardReportParamFiles` in `cypress.json`"
  ],
  "urls": [
    "{projectCode}/{orgUnitCode}/{dashboardGroupName}?report={reportId}&reportPeriod={startDate}-{endDate}"
  ]
}
