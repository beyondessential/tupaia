import { GeographicalAreaModel as CommonGeographicalAreaModel } from '@tupaia/database';

export class GeographicalAreaModel extends CommonGeographicalAreaModel {
  meditrakConfig = {
    minAppVersion: '0.0.23',
  };
}
