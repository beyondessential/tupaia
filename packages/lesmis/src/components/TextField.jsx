import { TextField as UiComponentsTextField } from '@tupaia/ui-components';
import styled from 'styled-components';

export const TextField = styled(UiComponentsTextField).attrs({ focusColor: '#ACACAC' })``;
