{
  "name": "@tupaia/export-chart-lambda",
  "version": "1.0.2",
  "description": "Lambda Function for Tupaia Chart Export",
  "main": "index.js",
  "scripts": {
    "package": "rm -f package.zip && yarn babel && cp -r package.json dist && cd dist && yarn --production && zip -rq ../package.zip . && cd .. && rm -rf dist",
    "babel": "rm -rf dist && mkdir dist && ./node_modules/.bin/babel src --out-dir dist --source-maps",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "core-worker": "^0.3.5",
    "nodemailer": "^4.4.2",
    "tar": "^4.0.1"
  }
}
