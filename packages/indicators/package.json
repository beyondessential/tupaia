{
  "name": "@tupaia/indicators",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "babel src --out-dir dist --ignore 'src/__tests__/**' --extensions '.ts' --config-file '../../babel.config.json'",
    "lint": "tsc; eslint . --ext .ts",
    "lint:fix": "yarn lint --fix",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@tupaia/aggregator": "1.0.0",
    "@tupaia/data-broker": "1.0.0",
    "@tupaia/database": "1.0.0",
    "case": "^1.5.3",
    "lodash.groupby": "^4.6.0"
  },
  "devDependencies": {
    "@tupaia/eslint-config-typescript": "1.0.0"
  },
  "eslintConfig": {
    "extends": "@tupaia/eslint-config-typescript"
  }
}
