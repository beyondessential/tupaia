'use strict';

var dbm;
var type;
var seed;

/**
 * We receive the dbmigrate dependency from dbmigrate initially.
 * This enables us to not have to rely on NODE_PATH.
 */
exports.setup = function (options, seedLink) {
  dbm = options.dbmigrate;
  type = dbm.dataType;
  seed = seedLink;
};

exports.up = async function (db) {
  await db.runSql(`
    CREATE TABLE entity_polygon (
      id           TEXT PRIMARY KEY DEFAULT generate_object_id(),
      polygon      geography(MultiPolygon,4326) NOT NULL,
      name         TEXT NOT NULL,
      code         TEXT,
      data_source  TEXT NOT NULL,
      created_at   TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now(),
      updated_at   TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()
    );
  `);

  await db.runSql('CREATE INDEX entity_polygon_code_idx ON entity_polygon(code);');

  // code is non-unique on its own (e.g. Laos and PNG enumerated areas may both
  // use "101"), but a (code, data_source) pair must be unique.
  await db.runSql(`
    CREATE UNIQUE INDEX entity_polygon_code_data_source_idx
      ON entity_polygon(code, data_source);
  `);

  await db.runSql(`
    ALTER TABLE entity ADD COLUMN entity_polygon_id TEXT
      REFERENCES entity_polygon(id) ON DELETE SET NULL;
  `);

  await db.runSql('CREATE INDEX entity_entity_polygon_id_idx ON entity(entity_polygon_id);');

  // Backfill: insert one entity_polygon row per entity-with-region (id is
  // generated by the column default), then link entity → entity_polygon via
  // the natural key (code, data_source).
  await db.runSql(`
    INSERT INTO entity_polygon (polygon, name, code, data_source)
    SELECT region, name, code, 'legacy'
    FROM entity
    WHERE region IS NOT NULL;
  `);

  await db.runSql(`
    UPDATE entity
    SET entity_polygon_id = ep.id
    FROM entity_polygon ep
    WHERE ep.code = entity.code AND ep.data_source = 'legacy';
  `);

  await db.runSql('ALTER TABLE entity DROP COLUMN region;');
};

exports.down = async function (db) {
  await db.runSql('ALTER TABLE entity ADD COLUMN region geography(MultiPolygon,4326);');

  await db.runSql(`
    UPDATE entity
    SET region = entity_polygon.polygon
    FROM entity_polygon
    WHERE entity.entity_polygon_id = entity_polygon.id;
  `);

  await db.runSql('ALTER TABLE entity DROP COLUMN entity_polygon_id;');
  await db.runSql('DROP TABLE entity_polygon;');
};

exports._meta = {
  version: 1,
  targets: ['server', 'browser'],
};
