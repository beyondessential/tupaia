export type Params = {
  sessionId: string;
};
export type ResBody = {
  totalToPull: number;
  pullUntil: number;
};
export type ReqBody = Record<string, never>;
export type ReqQuery = Record<string, never>;
